{
  "name": "fraud-fence",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "node --check api/analyze.js && node --check api/stream.js && node --check lib/config.js && node --check lib/transcribe.js && node --check lib/judge.js && node --check lib/risk.js && echo 'build check passed — no static build step needed'",
    "start": "vercel dev"
  },
  "dependencies": {
    "openai": "^4.68.0",
    "formidable": "^3.5.1"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}